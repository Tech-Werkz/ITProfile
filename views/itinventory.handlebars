<script>
    "use strict";
    console.log("🚀 IT Profile Plugin UI injected from views!");

    pluginHandler.registerPlugin({
        name: "ItInventory",
        
        onDeviceSetup: function() {
            console.log("IT Profile: onDeviceSetup function triggered!");
            if ($('#tabItInventory').length === 0) {
                $('#deviceTabs').append('<a id="tabItInventory" href="#" onclick="ItInventory_ShowTab()" class="nav-item nav-link">IT Profile</a>');
            }
        },
        
        onPluginMessage: function(msg) {
            if(msg.plugin === 'ItInventory' && msg.method === 'healthData') {
                $('#ItInventory_results').html('<pre>' + msg.data + '</pre>');
            }
        }
    });
</script>
